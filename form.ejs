<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>1RM Calculator • Rexx</title>
  <style>
    :root{
      --green:#16a34a;       /* 메인 초록 */
      --green-600:#16a34a;
      --green-700:#15803d;
      --green-50:#ecfdf5;
      --black:#0a0a0a;
      --muted:#6b7280;
      --radius:18px;
      --shadow:0 10px 30px rgba(0,0,0,.08);
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Noto Sans, Apple SD Gothic Neo, "맑은 고딕", sans-serif;
      color:var(--black);
      background:#fff;
      display:flex; align-items:center; justify-content:center;
    }

    /* 중앙 카드 */
    .card{
      width:min(680px, 92vw);
      background:#fff;
      border:1px solid #e5e7eb;
      border-radius:var(--radius);
      box-shadow:var(--shadow);
      padding:28px 28px 22px;
      position:relative;
    }

    /* 상단 헤더 */
    .brand{
      display:flex; align-items:center; gap:12px; margin-bottom:10px;
    }
    .brand svg{width:36px; height:36px}
    .brand-title{
      font-size:20px; font-weight:800; letter-spacing:.2px;
    }
    .subtitle{color:var(--muted); font-size:13px; margin-top:4px}

    /* 폼 */
    form{margin-top:18px}
    .grid{display:grid; grid-template-columns:1fr 1fr; gap:14px}
    @media (max-width:560px){ .grid{grid-template-columns:1fr} }

    label{
      display:block; font-size:13px; font-weight:700; margin:0 0 6px;
    }
    .input{
      width:100%;
      border:1.5px solid #e5e7eb;
      border-radius:12px;
      padding:12px 14px;
      font-size:16px;
      outline:none;
      transition:border .15s, box-shadow .15s;
    }
    .input:focus{
      border-color:var(--green-600);
      box-shadow:0 0 0 4px var(--green-50);
    }

    .row{margin-top:12px}
    .btn{
      width:100%;
      appearance:none;
      border:none;
      background:linear-gradient(180deg,var(--green-600),var(--green-700));
      color:#fff; font-weight:800; letter-spacing:.3px;
      padding:13px 16px; border-radius:12px; cursor:pointer;
      box-shadow:0 6px 16px rgba(22,163,74,.25);
      transition:transform .06s ease, filter .2s ease;
    }
    .btn:active{transform:translateY(1px)}
    .helper{font-size:12px; color:var(--muted); margin-top:10px}

    /* 하단 워터마크 */
    .watermark{
      position:fixed; left:50%; bottom:18px; transform:translateX(-50%);
      font-weight:900; letter-spacing:.15rem;
      color:#000; opacity:.08; user-select:none; pointer-events:none;
      text-transform:uppercase;
      font-size: clamp(14px, 2.5vw, 18px);
    }

    /* 결과 박스가 있다면 */
    .result{
      margin-top:16px; padding:14px 16px; border:1px dashed #d1fae5;
      border-radius:12px; background:#f0fdf4; color:#064e3b; font-weight:700;
    }

    /* 초록 라인 */
    .accent{
      height:6px; width:100%;
      background:linear-gradient(90deg, var(--green-700), var(--green-600));
      border-radius:10px; margin:8px 0 18px;
    }
  </style>
</head>
<body>

  <main class="card" role="main" aria-label="1RM 계산기">
    <div class="brand">
      <!-- 공룡 느낌의 심플 SVG -->
      <svg viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M10 45c0-9 6-16 18-16h10c7 0 11-3 13-9 1-2 3-2 5-1-1 10-6 15-14 16 6 2 9 7 9 15v6H36l-6-7-6 7H10v-11Z"
              fill="url(#g)"/>
        <circle cx="46" cy="18" r="2.2" fill="#0a0a0a"/>
        <defs>
          <linearGradient id="g" x1="10" y1="20" x2="58" y2="55" gradientUnits="userSpaceOnUse">
            <stop stop-color="#16a34a"/><stop offset="1" stop-color="#15803d"/>
          </linearGradient>
        </defs>
      </svg>
      <div>
        <div class="brand-title">Rexx 1RM Calculator</div>
        <div class="subtitle">흰 배경 · 초록 포인트 · 중앙 정렬</div>
      </div>
    </div>

    <div class="accent" aria-hidden="true"></div>

    <!-- 기존 서버에서 사용하는 action/method/name을 그대로 맞춰주세요 -->
    <form action="/calc" method="post" novalidate>
      <div class="grid">
        <div>
          <label for="weight">중량 (kg)</label>
          <input id="weight" name="weight" type="number" step="0.5" class="input"
                 value="<%= typeof weight !== 'undefined' ? weight : '' %>" required />
        </div>
        <div>
          <label for="reps">반복 횟수 (reps)</label>
          <input id="reps" name="reps" type="number" class="input"
                 value="<%= typeof reps !== 'undefined' ? reps : '' %>" required />
        </div>
      </div>

      <div class="row">
        <button class="btn" type="submit">계산하기</button>
        <div class="helper">Epley / Brzycki 등 공식을 서버에서 계산해 보여주세요.</div>
      </div>

      <% if (typeof result !== 'undefined') { %>
        <div class="result">예상 1RM: <%= result %> kg</div>
      <% } %>
    </form>
  </main>

  <!-- 하단 워터마크 -->
  <div class="watermark">rexx the dino</div>

</body>
</html>

